services:
  db:
    image: postgres:15
    container_name: postgres_db
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"

  app:
    build: .
    container_name: python_app
    depends_on:
      - db
    env_file:
      - .env